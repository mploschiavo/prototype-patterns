apiVersion: v1
kind: Service
metadata:
  name: web-nodeport
  namespace: csharp-hello-world-logger
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30091
