apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: python-hello-ingress
  namespace: python-hello-world
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: public
  rules:
    - host: python-hello.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web
                port:
                  number: 80
