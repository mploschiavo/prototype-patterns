apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: python-hello-logger-ingress
  namespace: python-hello-world-logger
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: public
  rules:
    - host: python-hello-logger.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web
                port:
                  number: 80
