apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-redis-single-rest
  namespace: integration-clients
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodejs-redis-single-rest
  template:
    metadata:
      labels:
        app: nodejs-redis-single-rest
    spec:
      containers:
        - name: app
          image: prototype/nodejs-redis-single-rest:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
            - name: REDIS_URL
              value: "redis://redis-single.integration-redis-single.svc.cluster.local:6379"
---
apiVersion: v1
kind: Service
metadata:
  name: nodejs-redis-single-rest
  namespace: integration-clients
spec:
  selector:
    app: nodejs-redis-single-rest
  ports:
    - name: http
      port: 8080
      targetPort: 8080
