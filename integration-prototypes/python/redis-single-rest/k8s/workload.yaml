apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-redis-single-rest
  namespace: integration-clients
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-redis-single-rest
  template:
    metadata:
      labels:
        app: python-redis-single-rest
    spec:
      containers:
        - name: app
          image: prototype/python-redis-single-rest:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          startupProbe:
            tcpSocket:
              port: 8080
            failureThreshold: 30
            periodSeconds: 2
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 20
          env:
            - name: PORT
              value: "8080"
            - name: REDIS_URL
              value: "redis://redis-single.integration-redis-single.svc.cluster.local:6379"
---
apiVersion: v1
kind: Service
metadata:
  name: python-redis-single-rest
  namespace: integration-clients
spec:
  selector:
    app: python-redis-single-rest
  ports:
    - name: http
      port: 8080
      targetPort: 8080
