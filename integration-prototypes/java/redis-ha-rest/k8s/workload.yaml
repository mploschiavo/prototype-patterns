apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-redis-ha-rest
  namespace: integration-clients
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java-redis-ha-rest
  template:
    metadata:
      labels:
        app: java-redis-ha-rest
    spec:
      containers:
        - name: app
          image: prototype/java-redis-ha-rest:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          startupProbe:
            tcpSocket:
              port: 8080
            failureThreshold: 30
            periodSeconds: 2
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 20
          env:
            - name: PORT
              value: "8080"
            - name: REDIS_CLUSTER_NODES
              value: "redis-ha-1.integration-redis-ha.svc.cluster.local:6379,redis-ha-2.integration-redis-ha.svc.cluster.local:6379,redis-ha-3.integration-redis-ha.svc.cluster.local:6379"
---
apiVersion: v1
kind: Service
metadata:
  name: java-redis-ha-rest
  namespace: integration-clients
spec:
  selector:
    app: java-redis-ha-rest
  ports:
    - name: http
      port: 8080
      targetPort: 8080
