apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-opensearch-single-rest
  namespace: integration-clients
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java-opensearch-single-rest
  template:
    metadata:
      labels:
        app: java-opensearch-single-rest
    spec:
      containers:
        - name: app
          image: prototype/java-opensearch-single-rest:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
            - name: OPENSEARCH_URL
              value: "http://opensearch-single.integration-opensearch-single.svc.cluster.local:9200"
---
apiVersion: v1
kind: Service
metadata:
  name: java-opensearch-single-rest
  namespace: integration-clients
spec:
  selector:
    app: java-opensearch-single-rest
  ports:
    - name: http
      port: 8080
      targetPort: 8080
