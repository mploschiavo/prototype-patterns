apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nodejs-hello-logger-ingress
  namespace: nodejs-hello-world-logger
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: public
  rules:
    - host: nodejs-hello-logger.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web
                port:
                  number: 80
