services:
  web:
    build:
      context: ../../
      dockerfile: Dockerfile
    image: prototype/java-hello-world:latest
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
    restart: unless-stopped
